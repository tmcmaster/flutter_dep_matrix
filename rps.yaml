scripts:
  update: dart clean; dart pub get
  deactivate: dart pub global deactivate flutter_dep_matrix
  activate:
    source: rps deactivate; dart pub global activate --source path .
    git: rps deactivate; dart pub global activate --source git git@github.com:tmcmaster/flutter_dep_matrix.git